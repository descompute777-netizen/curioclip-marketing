{
  "mcpServers": {
    "playwright": {
      "command": "cmd",
      "args": ["/c", "npx", "@playwright/mcp@latest", "--cdp-endpoint", "http://localhost:9222"],
      "description": "Playwright MCP — Browser automation conectado al Chrome con sesiones activas (TikTok, Composio, Pexels). Permite click, fill, navigate, screenshot via accessibility tree."
    },
    "playwright-standalone": {
      "command": "cmd",
      "args": ["/c", "npx", "@playwright/mcp@latest", "--browser", "chrome", "--user-data-dir", "C:/Users/Nick/AppData/Local/chrome-curioclip"],
      "description": "Playwright MCP standalone — Chrome con el perfil CurioClip (login guardado). Usar si la sesión CDP cae."
    },
    "sequential-thinking": {
      "command": "cmd",
      "args": ["/c", "npx", "-y", "@modelcontextprotocol/server-sequential-thinking"],
      "description": "Pensamiento Secuencial — razonamiento estructurado paso a paso"
    },
    "context7": {
      "command": "cmd",
      "args": ["/c", "npx", "-y", "@upstash/context7-mcp"],
      "description": "Context7 — documentación actualizada de librerías para LLMs"
    },
    "composio-tiktok": {
      "type": "sse",
      "url": "https://mcp.composio.dev/composio/mcp?apiKey=ck_NcIb61zkczdt9WOrGTYQ",
      "description": "Composio MCP — TikTok (requiere conectar TikTok en app.composio.dev/settings/connected_accounts)"
    },
    "tiktok-ads-pipeboard": {
      "type": "sse",
      "url": "https://tiktok-ads.mcp.pipeboard.co/",
      "description": "TikTok Ads MCP via Pipeboard — campañas y analytics de ads"
    }
  }
}
